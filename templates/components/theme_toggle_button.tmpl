{{define "components/theme_toggle_button" -}}
<button class="{{.Class}}" type="button" onclick="globalThis.mrnToggleTheme && globalThis.mrnToggleTheme()"{{if .Disabled}} disabled{{end}}>🌓 Theme</button>
{{- end}}
