{{define "components/theme_toggle_button" -}}
<button class="{{.Class}}" type="button" onclick="window.mrnToggleTheme && window.mrnToggleTheme()"{{if .Disabled}} disabled{{end}}>🌓 Theme</button>
{{- end}}
